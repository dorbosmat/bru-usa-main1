# Dependencies
node_modules

# Build output
dist
coverage

# Local env / secrets
# SECURITY-TODO: .env is currently tracked in git history (publishable keys
# only, no real secrets). Untracking it requires `git rm --cached .env`,
# which would delete it from collaborators' working copies on next pull.
# Decide as a team before doing so. Listing it here prevents *new* env
# additions from being auto-staged in the meantime.
.env
.env.*
!.env.example

# OS / editor noise
.DS_Store
Thumbs.db
.vscode
.idea
*.swp
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Misc
.cache

# Supabase CLI cache (auto-regenerated; not sensitive)
supabase/.temp/
